// literal
// 리터럴은 사람이 이해할 수 있는 문자 또는 약속된 기호를 사용해 값을 생성하는 표기법이다.
 
// 리터럴 표현식
10;
'hello';

// 식별자 표현식
let sum;
sum;

const person = {name : 'kang'};
person.name;

let arr = [1, 2, 3];
arr[1];

//연산자 표현식
10 + 20;
sum = 10;
sum !== 10;

//함수, 메서드 호출 표현식 (선언이 이미 존재한다고 가정)
square();
person.getname();

// 산술 연산자
let x = 1 + 2;

x + 3; // 식별자 표현식 x는 3으로 평가된다.
// 1 + 2 는 3과 같기 때문에, 1 + 2도 평가되어 3과 같은 표현식이 된다.